---
name: sync
description: "Sync local .agent-memory/ with global ~/.claude-memory/global/ for cross-project knowledge sharing."
user_invocable: true
---

# Cross-Project Sync

Manually trigger a sync between local project memory and the global memory store.

## Instructions

Invoke the `agentic-os:sync-context` skill.

Use the Skill tool:
```
Skill: agentic-os:sync-context
```
