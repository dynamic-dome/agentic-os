{
  "description": "Agentic OS Lifecycle Hooks v6: Auto-Init (command), Intent Routing (prompt), Lightweight Logging (prompt), Context Preservation (prompt), Wrap-Up Delegation (prompt), Agent Commit Suggest (prompt)",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/session-start.sh\"",
            "timeout": 15
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Scan the user's prompt for intent signals. Do NOT act on these — only add a brief internal hint if relevant:\n\n- If the prompt describes completed work (e.g. 'fixed the bug', 'feature is done', 'refactored X') → hint: 'Consider logging this iteration with iteration-logger.'\n- If the prompt mentions a decision or tech choice (e.g. 'let's use Redis', 'switching to TypeScript') → hint: 'Consider recording this decision with context-keeper.'\n- If the prompt asks about recurring problems (e.g. 'why does this keep happening', 'same error again') → hint: 'Consider running pattern-extractor.'\n\nIf no signal is detected, return 'approve' silently. Never block or delay the user. This is advisory only.\n\nReturn 'approve'.",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "You are the Agentic OS Stop hook (lightweight). Check if `.agent-memory/` exists. If not, return 'approve' immediately.\n\nIf it exists and meaningful code changes were made this turn:\n1. Append a brief iteration entry to `.agent-memory/iterations/iteration-log.md` (date, type, 1-line summary).\n2. If errors occurred, append to `.agent-memory/iterations/errors.json`.\n3. Check if `improvements/state.json` exists and has `status: \"running\"`. If so, warn: 'Self-improvement loop is still active — wait for completion or check /agentic-os:status'.\n\nDo NOT update session-summary.md here — that is handled by SessionEnd.\nDo NOT trigger pattern-extractor — that is handled by wrap-up skill.\n\nReturn 'approve'.",
            "timeout": 15
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Context is about to be compressed. If `.agent-memory/` exists, read these files and OUTPUT a compact survival summary (this text will persist after compression):\n\n1. Read `.agent-memory/session-summary.md` → output: 'LAST SESSION: {1-line summary}'\n2. Read `.agent-memory/context/project-context.md` → output: 'PROJECT: {name} | STACK: {compact stack}'\n3. Read `.agent-memory/open-tasks.json` → output: 'OPEN TASKS: {list each task ID + title on one line}' — **CRITICAL: every open/blocked task MUST appear here. These must survive compression.**\n4. Read `.agent-memory/identity/soul.md` → output: 'IDENTITY: {language preference}, {brevity level}'\n5. Note current git branch → output: 'BRANCH: {branch name}'\n\nThe goal is to produce a readable text block (not just read files silently) so the compressed context retains this information. Return 'approve'.",
            "timeout": 15
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Session is ending. If `.agent-memory/` does not exist, return 'approve' immediately.\n\nIf it exists:\n1. **TASK PERSISTENCE GUARD (MANDATORY):** Read `.agent-memory/open-tasks.json`. If it exists, read ALL tasks with status 'open' or 'blocked'. Verify they appear in the 'Open Items' section of `.agent-memory/session-summary.md`. If any are missing, add them now. Never drop, skip, or summarize tasks away.\n2. **DELEGATE TO WRAP-UP:** If wrap-up has NOT already run this session (check if session-summary.md Date line contains today's date), invoke `agentic-os:wrap-up` for full session summary, learnings, and pattern extraction.\n3. If wrap-up already ran, only ensure task persistence (Step 1) and return.\n\nReturn 'approve'.",
            "timeout": 15
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "matcher": "quality-gate|improvement-agent",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "An agent just completed a significant task. Based on the agent's output and any files it reported changing, determine if there are meaningful code changes (not just .agent-memory/ updates).\n\nIf meaningful code changes were made:\n1. Suggest a commit message based on the changes.\n2. Ask the user: 'Should I commit these changes? Suggested message: {message}'\n3. Only commit if the user confirms.\n\nIf only .agent-memory/ files changed, or if the agent made no file changes, skip silently.\n\nReturn 'approve'.",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
